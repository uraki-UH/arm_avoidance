# 把持位置・把持対象サイズ推定の現状

- 整理日: 2026-09-11
- 資料化の目安: 2026-09-14（月）
- 対象: `grasp_new4` の現在の実装
- 今回の中心: 把持位置候補の生成と、把持対象の局所サイズ・ハンドへの適合評価
- 今回の対象外: 軌道生成、ロボット移動、実機での持ち上げ動作

実装・設定・既存の検証記録に基づく概要。本資料作成時の実行再検証・動画撮影は未実施。

## 1. 全体像

**観測点群をGNGでグラフ化し、表面のまとまりやハンド形状との適合から、把持できそうな位置・向きと局所サイズを求める構成。**

```text
対象・環境の点群
  -> ROS 2版GNG: ノード・エッジ・法線など
  -> 平面クラスタ -> 局所サイズ・突出量の判定 -> 上方向の把持候補
  または
  -> 対象ボクセル + ハンド領域 -> 占有・接触候補・干渉の照合 -> 把持候補
  -> Viewerで点群・形状・候補姿勢を重ねて表示
```

2つの候補生成方式は独立した経路。平面方式の候補が自動でボクセル方式へ渡る構成ではない。

## 2. 現状できること

| 機能 | 現在の実装内容 | 発表・デモで示す内容 |
| --- | --- | --- |
| 点群からの形状表現 | ROS 2版CPU GNGによるグラフ学習、局所法線などの算出 | 元点群とGNGの対応 |
| 平面領域の抽出 | GNGからの平面クラスタ生成、`/plane_clusters`への出力 | 物体表面の分割結果 |
| 上方向の把持位置推定 | 各平面クラスタの投影外接矩形と隣接平面からの突出量による候補選別 | 採用候補の位置・進入方向 |
| 局所サイズの算出 | 候補領域の幅・奥行き、表面高さの出力 | ハンドに収まる大きさかの説明 |
| ハンド形状の表現 | 最大把持領域、最小把持領域、基部・開閉時の禁止領域のグラフ生成 | 指の内側と干渉領域の区別 |
| ボクセルによる候補生成 | 対象占有、両側の接触候補、最小領域外の支持、禁止領域占有の照合 | 形状とハンドの関係に基づく候補比較 |
| 候補の根拠出力 | 候補数、棄却理由、サイズ、占有率、接触法線の整合など | 「なぜ候補になったか」の表示 |
| 可視化 | GNG、平面領域、ハンド領域と、到達性評価側の候補MarkerのViewer表示経路 | 画面収録による処理段階の比較 |

いずれも「実装が存在する」の意味であり、任意物体への頑健性や実機把持成功の保証ではない。

## 3. 把持位置推定

### 平面クラスタ方式: 今回の主デモ候補

アルゴリズム・数式・設定・制約の詳細は、[局所平面クラスタを利用した上方把持候補推定](grasping_system/docs/top_grasp_surface_estimation.md)を参照。

1. 各平面クラスタの所属ノードを、上方向に直交する平面へ投影。
2. 主軸に沿った外接矩形（OBB）から、幅・奥行きと向きを算出。
3. ハンドの把持面へ収まるか、隣接平面から十分に突出しているかを判定。
4. 採用領域の矩形中心と表面高さからTCP位置を生成。TCPのローカルZ軸は下向き。

- 隣接する壁などを候補の矩形へまとめず、クラスタごとに独立評価。
- 隣接平面がない場合は突出量条件を適用しないため、孤立領域の誤採用には注意。
- 現方式は法線の向きによる上面限定ではなく、投影サイズと突出量による判定。
- 候補は接触・摩擦・重心まで検証した把持成功姿勢ではない。

### ボクセル方式: 比較・補足デモ候補

- 対象ボクセルへハンド領域を配置し、設定した姿勢集合を照合。
- 最大領域内の占有、最小領域だけに収まる小さすぎる対象の除外、両側の接触候補、禁止領域への侵入を評価。
- 接触候補の法線整合や平面由来の支持割合も診断情報として出力。
- 観測されていない場所の占有は不明。現在の共通設定では深度可視性評価が無効のため、禁止領域に点がないだけで安全とは断定不可。

## 4. サイズ推定として説明できる範囲

**現状の主な出力は「把持候補となる局所領域の大きさ」。遮蔽を補完した物体全体の縦・横・高さとは別物。**

| 出力・評価 | 意味 | 注意点 |
| --- | --- | --- |
| `extent_x`, `extent_y` | 候補領域の投影OBB寸法 [m] | GNG点間の補正量を両側に加えた寸法 |
| `surface_height` | 設定した上方向への表面位置 [m] | 物体の厚さ・全高ではない |
| `footprint_fill_ratio` | 使用可能な把持面積に対するOBB面積比 | 点群の充填率や把持成功確率ではない |
| 最大・最小把持領域との照合 | 観測形状に対するハンド適合の幾何評価 | 物体全体の実寸復元とは異なる |

- 上記の局所寸法は `/grasp_pose_cands/summary` の採用候補ごとに出力。
- 別系統の計画用評価メッセージにある `gripper_width` と `grasp_region_score` は、現在の生成処理ではNaN。完成済みの開口幅推定として紹介しない。
- 欠損による過小評価、ノード密度・平面分割による寸法変動、既知寸法に対する誤差は、撮影前に確認したい項目。
- テンプレート照合による未観測部分の補完や、物体全体のサイズ推定精度は、本資料では検証済み成果に含めない。

## 5. デモ動画案

1. 入力点群だけを表示し、物体形状と観測条件を提示。
2. GNGと平面クラスタを順に重ね、把持推定に使う表面領域を提示。
3. 上方向の把持候補を重ね、候補位置・向き・局所寸法を提示。
4. 寸法の異なる対象、または突出の有無が異なる配置で、採用・棄却の違いを比較。
5. 余裕があればハンド領域とボクセル方式の候補を追加し、接触候補・干渉の考え方を説明。

軌道や関節駆動は不要。寸法の画面表示は専用GUIの完成を前提にせず、summaryの表示または動画字幕で補足。

撮影前の確認事項:

- 入力点群、GNG、平面クラスタ、候補Markerの継続受信と、Viewerの安定表示。
- 座標系・TF・長さ単位、および上方向の一致。
- `ToPoDualArm.yaml`の現設定は把持面 `0.15 x 0.15 m`、内側余白 `0.002 m`、点間補正 `0.003 m`。使用ハンドとデモの想定寸法との整合確認。
- 同じ静止対象での候補位置・寸法の揺らぎ、既知寸法との差の記録。
- 「候補が出る例」だけでなく、過大領域や突出不足で棄却される例も用意。

## 6. 主デモの起動メモ

ROS 2パッケージのビルド・環境設定済みの端末で実行。Docker内の場合も同じROSコマンドで、以下のパスは `/ros2_ws/src` への配置を前提とする。点群送信側とViewer/bridgeは別途起動が必要。既に稼働しているノードは重複起動しない。

点群を `/semantic_points` で送信した状態で、GNG学習を起動:

```bash
ros2 launch ais_gng ais_gng.launch.py \
  backend:=cpu lidar:=graspnet.yaml input_topic:=/semantic_points
```

別端末で上方向の把持候補生成を起動:

```bash
ros2 launch grasping_system top_grasp_surface_estimator.launch.py \
  params_file:=/ros2_ws/src/gng_vlut_system/config/ToPoDualArm.yaml
```

主な確認トピックは `/topological_map`、`/plane_clusters`、`/grasp_pose_cands`。候補の可視化は `grasp_goal_planning.launch.py` 側の `/grasp_pose_cands/reachability_markers` を利用し、候補生成側からの重複Marker配信はなし。上方方式とボクセル方式の候補生成は同じ出力先のため、どちらか一方だけ起動。サイズ・棄却理由の確認:

```bash
ros2 topic echo /grasp_pose_cands/summary \
  --qos-durability transient_local --once
```

この2つのlaunchは候補計算に軌道生成用launchを必要としないが、単独では候補の矢印表示を出力しない。到達性評価付き表示を使う場合は `grasp_goal_planning.launch.py enable_motion:=false` を別途起動。この場合、動作指令は無効でも経路計算は有効。撮影終了時は自分で起動したノードを各端末のCtrl+Cで停止。

## 7. スライドの骨子と未検証事項

1. 目的: 点群から把持位置と把持対象の局所サイズを推定。
2. 表現: GNGによる形状グラフと平面クラスタ。
3. 推定: 領域サイズ・突出量とハンド形状による候補評価。
4. デモ: 点群、推定領域、候補、局所寸法の重ね合わせ。
5. 課題: オクルージョン、分割の安定性、サイズ誤差、接触・把持成功の検証。

既存の2026-08-27検証記録には、GraspNet scene 3で候補1〜2件、候補推定部分0.06〜0.07 msの報告あり。ただし過去の設定・環境での値であり、GNG・通信・描画を含む全体時間でも、現在の再測定値でもない。

ファジールール候補集は今後の拡張案を含むため、全項目が実装済みとは扱わない。物体全体の寸法精度や実機把持成功率も未検証事項として明示。

## 参照先

- [把持機能の概要](grasping_system/README.md)
- [上方向把持の実装](grasping_system/include/candidate/top_grasp_surface_estimator.hpp)
- [上方向把持のROS出力](grasping_system/src/top_grasp_surface_estimator_node.cpp)
- [ボクセル方式のROS実装](grasping_system/src/grasp_voxel_matcher_node.cpp)
- [現在の共通設定](gng_vlut_system/config/ToPoDualArm.yaml)
- [上方向把持の既存検証記録](gng_vlut_system/docs/releases/2026-08-27_top_grasp_surface_estimation.md)
- [今後のファジールール候補](FUZZY_GRASP_RULE_CATALOG.md)
