# 2026-09-02 作業まとめ

## 1. 要約

| 対象 | 当日の到達点 |
| --- | --- |
| HTML点群・GNG | GNG-WASMへ統一。「GNG再生」に学習操作を集約。物体・机・遮蔽点群と配置操作を改善 |
| 平面・非平面成分 | CPU平面抽出後に未所属ノードの連結成分を生成。`TopologicalNode.nonplane_component_id`へ所属を統合 |
| Viewer・照合UI | Matchタブとparameter調整RPCを追加。frame別TFと非平面成分のsource認識に対応 |
| 入力・評価準備 | Gazebo設定をYAML化。registration検証環境とFoundationPose用RGB-D出力を整備 |

非平面成分は形状を重複送信せず、所属情報だけを配信。専用message・旧`nonplane_component_node`・専用Markerトピックを廃止。

| 情報 | 保持先 |
| --- | --- |
| ノード・辺・座標 | `/topological_map` |
| 平面クラスタ | `/plane_clusters` |
| 非平面成分ID | `TopologicalNode.nonplane_component_id` |
| 非平面成分の所属一覧 | `/nonplane_components`（`UInt32MultiArray`） |

所属payloadは`[frame_number, component_num, component_id, node_num, node_index..., ...]`。Viewer・matcherは既存mapから形状と辺を復元。

## 2. 条件・検証

- 対象は2026-09-02のGit履歴`3ec0735`〜`c9f30fd`の11 commitと、commit後の実機相当入力確認。当時の記録であり、現在の完了状況の再調査ではない。
- `ais_gng_msgs`・`ais_gng`・`topo_fuzzy_viewer`の`colcon build --symlink-install`、非平面成分抽出のCTestが成功。
- 3トピックの接続・所属payload受信、旧message・ノードの公開終了、Viewerで191ノードの受信を確認。
- HTML側はWASMビルド、構文、headless browserの初期表示、点群密度を確認。

**当時の未完了事項**

- 非平面成分のノード・成分内辺・平面anchor辺を含む最終描画確認。
- 成分ID単位の照合、遮蔽による未観測の中立扱い、複数平面をまたぐ持ち手評価。
- Gazebo点群から把持候補までの一連動作確認。

根拠：[非平面成分統合](releases/2026-09-02_nonplane_component_topological_node.md)、[照合UI](releases/2026-09-02_topofuzzy_object_template_match_tuner.md)、[WASM既定化](releases/2026-09-02_html_wasm_gng_default.md)、[遮蔽照合の見積り](releases/2026-09-02_occlusion_template_matching_cost_estimate.md)。
